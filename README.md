# Curves Project

C++ implementation of 3D curves (Circle, Ellipse, Helix).

## Features
- Circle, Ellipse, Helix with virtual methods
- Random curve generation
- Point and derivative calculation
- Sorting circles by radius
- Sum of radii

## Build
```bash
mkdir build
cd build
cmake ..
cmake --build .
```

